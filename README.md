# go-math-archive-test-lib

Small test library for validating GitHub archive behavior with Go modules.

It also exposes a root command so `go install github.com/rbell23/go-math-archive-test-lib@<version>` installs an executable for archive testing.
